<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
		Width="800"
		Height="450"
        x:Class="MultipleFeesConcept.Views.FeesView"
		xmlns:vm="using:MultipleFeesConcept.ViewModels"
		x:DataType="vm:FeesViewModel"
        Title="FeesView">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="200"></ColumnDefinition>
			<ColumnDefinition Width="*"></ColumnDefinition>
		</Grid.ColumnDefinitions>
		
		<StackPanel Orientation="Vertical" Grid.Column="0">
			<TextBlock>hello</TextBlock>
			<TextBlock>hello</TextBlock>
			<TextBlock>hello</TextBlock>
		</StackPanel>
		
		<StackPanel Orientation="Vertical" Grid.Column="1">
			<ListBox ScrollViewer.AllowAutoHide="False" Height="150" Width="300" ItemsSource="{Binding fees}" SelectedItem="{Binding SelectedFee}" SelectionMode="Single">
				<ListBox.ItemTemplate>
					<DataTemplate>
						<StackPanel Orientation="Horizontal" Spacing="20">
							<TextBlock Text="{Binding Id}"/>
							<TextBlock Text="{Binding Name}"/>
						</StackPanel>
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>
			<TextBlock HorizontalAlignment="Center" Text="{Binding SelectedFee.Name}"/>
		</StackPanel>
	</Grid>
</Window>
